# Привет, Claude!

Мы работаем над проектом **UniAudit** (B2B SaaS для аудита сайтов университетов с помощью ИИ). Я (OpenClaw) недавно занимался настройкой архитектуры, фиксом CORS/Mixed Content для продакшена (Vercel -> Azure VM) и частичным переносом дизайн-системы (стиль Clawforge).

Пользователь (Tair) передает эстафету тебе для дальнейшей работы локально. 

**Твоя главная задача сейчас — довести проект до ума (сделать из него премиальный B2B продукт), улучшить UI/UX и добавить функционал для аутрича.**

Перед началом работы:
1. **Прочитай `ARCHITECTURE.md`** — там описан текущий стек, структура БД и как работает проксирование запросов в продакшене.
2. **Прочитай `CLAUDE_HANDOVER.md`** — там подробно описаны 4 главные цели (Objectives), на которых нужно сфокусироваться прямо сейчас (Полировка UI, фикс страницы Brandbook, генератор холодных писем для Deans и добавление Error Boundaries).

**Чего ждет пользователь:**
- Глубокой проработки UI на базе Tailwind (используй только семантические цвета `bg-background`, `bg-card`, `border-border` и т.д., никаких хардкодов).
- Чтобы приложение перестало падать "в белый/черный экран" при малейшей ошибке в данных (нужны `ErrorBoundary` и Fallback-скелетоны).
- Чистого, модульного и переиспользуемого кода React.

*Обрати внимание: Я почистил репозиторий от старых логов и временных файлов. Можешь смело приступать к задачам из `CLAUDE_HANDOVER.md` (Objective 1-4).*

Удачи! Действуй автономно, коммить часто и осмысленно.
